[concept]
id = "parser.quote_like.custom_delimiter_pair"
tier = "pr"

[expect]
panic = false
timeout = false
mode = "token_only"

[metrics]
max_error_nodes = 0
must_emit_node_kinds = ["QuoteLike", "RegexQuoteLike"]

[snapshots]
tokens = true
ast = false
spans = false
