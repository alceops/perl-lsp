name: fixture-pr-target-checkout-head
on:
  pull_request_target:
    types: [opened, synchronize]
permissions:
  contents: read
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - run: echo "unsafe"
